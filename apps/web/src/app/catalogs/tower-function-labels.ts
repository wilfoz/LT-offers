import { TowerFunction } from '@lt-offers/domain';

// Rótulos pt-BR das funções de tipo de torre (RNF-14)
export const TOWER_FUNCTION_LABELS: Record<TowerFunction, string> = {
  SUSPENSION: 'suspensão',
  ANCHOR: 'ancoragem',
};
